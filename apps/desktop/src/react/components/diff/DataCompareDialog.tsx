"use client";

// Placeholder for DataCompareDialog component
// Full migration pending

interface DataCompareDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function DataCompareDialog({ open, onOpenChange }: DataCompareDialogProps) {
  return null;
}

// Placeholder for SchemaDiffDialog component
interface SchemaDiffDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function SchemaDiffDialog({ open, onOpenChange }: SchemaDiffDialogProps) {
  return null;
}