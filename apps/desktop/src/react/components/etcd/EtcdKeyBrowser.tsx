"use client";

// Placeholder for EtcdKeyBrowser component
// Full migration pending

interface EtcdKeyBrowserProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function EtcdKeyBrowser({ open, onOpenChange }: EtcdKeyBrowserProps) {
  return null;
}