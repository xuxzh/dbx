"use client";

// Placeholder for RedisKeyBrowser component
// Full migration pending

interface RedisKeyBrowserProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function RedisKeyBrowser({ open, onOpenChange }: RedisKeyBrowserProps) {
  return null;
}

// Placeholder for RedisValueViewer component
// Full migration pending

interface RedisValueViewerProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function RedisValueViewer({ open, onOpenChange }: RedisValueViewerProps) {
  return null;
}

// Placeholder for RedisPubSubPanel component
// Full migration pending

interface RedisPubSubPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function RedisPubSubPanel({ open, onOpenChange }: RedisPubSubPanelProps) {
  return null;
}