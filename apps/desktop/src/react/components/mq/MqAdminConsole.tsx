"use client";

// Placeholder for MonitoringPanel component
// Full migration pending

interface MonitoringPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function MonitoringPanel({ open, onOpenChange }: MonitoringPanelProps) {
  return null;
}

// Placeholder for MqAdminConsole component
// Full migration pending

interface MqAdminConsoleProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function MqAdminConsole({ open, onOpenChange }: MqAdminConsoleProps) {
  return null;
}

// Placeholder for NamespacesPanel component
// Full migration pending

interface NamespacesPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function NamespacesPanel({ open, onOpenChange }: NamespacesPanelProps) {
  return null;
}

// Placeholder for PermissionsPanel component
// Full migration pending

interface PermissionsPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function PermissionsPanel({ open, onOpenChange }: PermissionsPanelProps) {
  return null;
}

// Placeholder for PoliciesPanel component
// Full migration pending

interface PoliciesPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function PoliciesPanel({ open, onOpenChange }: PoliciesPanelProps) {
  return null;
}

// Placeholder for ProducerConsumerPanel component
// Full migration pending

interface ProducerConsumerPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function ProducerConsumerPanel({ open, onOpenChange }: ProducerConsumerPanelProps) {
  return null;
}

// Placeholder for RawApiPanel component
// Full migration pending

interface RawApiPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function RawApiPanel({ open, onOpenChange }: RawApiPanelProps) {
  return null;
}

// Placeholder for SubscriptionsPanel component
// Full migration pending

interface SubscriptionsPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function SubscriptionsPanel({ open, onOpenChange }: SubscriptionsPanelProps) {
  return null;
}

// Placeholder for TenantsPanel component
// Full migration pending

interface TenantsPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function TenantsPanel({ open, onOpenChange }: TenantsPanelProps) {
  return null;
}

// Placeholder for TopicsPanel component
// Full migration pending

interface TopicsPanelProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function TopicsPanel({ open, onOpenChange }: TopicsPanelProps) {
  return null;
}