"use client";

// Placeholder for FieldLineageDialog component
// Full migration pending

interface FieldLineageDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function FieldLineageDialog({ open, onOpenChange }: FieldLineageDialogProps) {
  return null;
}