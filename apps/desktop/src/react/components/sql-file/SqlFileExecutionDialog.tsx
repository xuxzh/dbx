"use client";

// Placeholder for SqlFileExecutionDialog component
// Full migration pending

interface SqlFileExecutionDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function SqlFileExecutionDialog({ open, onOpenChange }: SqlFileExecutionDialogProps) {
  return null;
}