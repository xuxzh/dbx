"use client";

// Placeholder for DatabaseSearchDialog component
// Full migration pending

interface DatabaseSearchDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function DatabaseSearchDialog({ open, onOpenChange }: DatabaseSearchDialogProps) {
  return null;
}