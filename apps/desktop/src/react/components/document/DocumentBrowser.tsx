"use client";

// Placeholder for DocumentBrowser component
// Full migration pending

interface DocumentBrowserProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function DocumentBrowser({ open, onOpenChange }: DocumentBrowserProps) {
  return null;
}