"use client";

// Placeholder for TableImportDialog component
// Full migration pending

interface TableImportDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function TableImportDialog({ open, onOpenChange }: TableImportDialogProps) {
  return null;
}