"use client";

// Placeholder for DatabaseExportDialog component
// Full migration pending

interface DatabaseExportDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function DatabaseExportDialog({ open, onOpenChange }: DatabaseExportDialogProps) {
  return null;
}

// Placeholder for ExportProgressDialog component
interface ExportProgressDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function ExportProgressDialog({ open, onOpenChange }: ExportProgressDialogProps) {
  return null;
}