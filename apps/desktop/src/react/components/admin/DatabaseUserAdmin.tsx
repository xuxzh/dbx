"use client";

// Placeholder for DatabaseUserAdmin component
// Full migration pending

interface DatabaseUserAdminProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function DatabaseUserAdmin({ open, onOpenChange }: DatabaseUserAdminProps) {
  return null;
}