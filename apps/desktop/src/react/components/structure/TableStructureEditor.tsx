"use client";

// Placeholder for TableStructureEditor component
// Full migration pending

interface TableStructureEditorProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function TableStructureEditor({ open, onOpenChange }: TableStructureEditorProps) {
  return null;
}