"use client";

// Placeholder for DataGenerateDialog component
// Full migration pending

interface DataGenerateDialogProps {
  open: boolean;
  onOpenChange: (open: boolean) => void;
}

export function DataGenerateDialog({ open, onOpenChange }: DataGenerateDialogProps) {
  return null;
}